---
const picture = Astro.props.picture;
const alttext = Astro.props.alttext;
const headline = Astro.props.headline;
const description = Astro.props.description;
---

<article class="bg-blue-500 text-dark-500 m-8 p-8 rounded-lg basis-1/3">
    <img src={picture} alt={alttext} class="w-100">
    <h3 class="text-2xl p-2 font-bold">{headline}</h3>
    <p class="p-2">{description}</p>
    <a href="#" class="hover:rounded hover:bg-white-800 p-4 text-2xl font-semibold text-center underline">Read the story</a>
</article>

<style>
    article {
        display: flex;
        flex-direction: column;
        }
        article a {
            margin-top: auto;
        }
</style>